# Emma's Mobile Beauty & Holistic Wellness
Static site. Deploy: import this folder as a GitHub repo, then "Add New Project" on Vercel (no build step, output dir = root).
Admin login: emma / 1111 (change before going public). Next: Twilio SMS + database for shared bookings + Meta Pixel ID in index.html head.
